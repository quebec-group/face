package uk.ac.cam.cl.quebec.face.storage;

/**
 * Handles persistent storage of results from face recognition, for recovery later.
 */
public class RecognitionResultFiles {
    // File path spec: dataDir/results/{eventId}/{videoId}.json
}
